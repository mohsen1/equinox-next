#!/usr/bin/env bash
set -euo pipefail

repository_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"$repository_root/scripts/ensure-local-env"
exec "$repository_root/.venv/bin/python" -m research.runpod.study_operator "$@"
